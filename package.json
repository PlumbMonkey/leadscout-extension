{
  "name": "leadscout",
  "version": "1.0.0",
  "private": true,
  "description": "LeadScout – Chrome Extension + Local Server for lead capture, scoring & outreach",
  "workspaces": [
    "shared",
    "server",
    "extension",
    "hunter"
  ],
  "scripts": {
    "build:shared": "cd shared && npm run build",
    "build:server": "cd server && npm run build",
    "build:extension": "cd extension && npm run build",
    "build:hunter": "cd hunter && npm run build",
    "build": "npm run build:shared && npm run build:server && npm run build:extension && npm run build:hunter",
    "dev:server": "cd server && npm run dev",
    "start:server": "cd server && npm start",
    "dev:hunter": "cd hunter && npm run dev",
    "hunter": "cd hunter && npm run hunter"
  }
}
